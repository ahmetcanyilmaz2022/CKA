# Bu ServiceAccount, CI/CD pipeline’ı için kullanılacak kimliktir.
# Pod veya dış araçlar (örneğin Jenkins, ArgoCD, GitHub Actions) bu hesabı kullanarak API’ye erişebilir.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cicd-token
  namespace: app-team1

# Kubernetes’te ServiceAccount, bir pod’un Kubernetes API’siyle konuşurken kim olduğunu belirten bir
#“kullanıcı hesabı” gibidir.

# Normal kullanıcılar (örneğin sen) kubectl ile giriş yaparsın.
# Pod içindeki uygulamalar da bazen API’ye erişmek ister (örnek: başka pod’ların bilgisini almak).
# İşte bu durumda, pod’lara bir ServiceAccount atanır.
# Bu hesap, pod’a bir token verir ve bu token ile API server’a kimlik doğrulaması yapılır.
#
# 👉Özetle: ServiceAccount = Pod’un Kubernetes içindeki pasaportu
# Pod’un ne yapabileceğini belirlemek için genellikle Role veya ClusterRole ile birlikte RoleBinding yapılır.
